services:
  n5deal-marketplace:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: n5deal-marketplace
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
